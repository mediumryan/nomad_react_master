import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
    textColor: '#000',
    bgColor: '#E9E4ED',
    boxColor: '#fff',
    accentColor: '#61398F',
};

export { theme };
